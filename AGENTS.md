# 工作准则（全局 · 跨会话）

> **本文件是唯一准则。任何新会话开始，先读本文件 → 再读 `plan/progress.md` → 再读 `plan/daily/` 最近 3 个文件，然后才动手。**
> 最后更新：2026-09-08（路线图升级 v2：方向定为 AI 存储 + 框架优化；新增实习时间窗 2027 寒假）

---

## 1. 关于使用者

- **2026 级研一，2029 年毕业**。**目标：2027 年寒假（约 2027-01 中旬）拿到第一段 AI Infra 实习** —— 这是当前所有计划的时间锚点
- 研究方向 **LLM Agent 的 Memory 系统**（跟进 MemoryBank / Mem0 / MemGPT / ReadAgent / A-Mem / Zep-Graphiti / MemOS / GraphRAG 等），已收敛到**存储 / 系统视角**
- 系统 / 数据库背景（有列存数据库实习；另做 Gamma 向量化 PostgreSQL 扩展的向量存储与压缩），**正在转向 AI Infra**
- 手上有 NVIDIA GPU（型号 / 显存 / CUDA 版本**待确认**，阻塞 Phase 1）
- 导师派的**无人机算法竞赛横向**（平台 AirSim），内容水、不想深入，每周 1~2 天应付
- 沟通偏好：直接给判断，不要客套铺垫；结构化输出；结论先行；有反对意见直说

---

## 2. 对我（助手）的硬约束

1. **不写客套话**。不说"好的呢""很高兴帮你""这是一个很好的问题"。
2. **禁止编造**。论文标题、数字、结论、课程信息不确定时必须显式标注"未核实 / 不确定"，宁可说不知道。
3. **给建议要给优先级和理由**，不要罗列一堆选项让用户自己挑。
4. **有判断就直说**，包括"这个方向不值得做""这个计划太满了"。
5. **改了系统状态必须同步改文件**，不能只在对话里说。
6. **中文**。
7. 每次动项目前先确认当前阶段（`plan/progress.md` 的勾选状态），不要凭印象。

---

## 3. 三条线：定位与优先级

| 线 | 定位 | 每周投入 | 成功标准 |
|---|---|---|---|
| **1. Agent Memory 论文** | 主战场，决定毕业 | ~8h | 每天 ≥1 条 idea；每周 ≥1 篇精读；每月 1 份 open problems；**每篇必问"存储开销 / 规模化代价"** |
| **2. AI Infra（主线 A 存储 + 主线 B 框架）** | **决定 2027 寒假实习，当前最高优先级** | ~12h | fio/IO 路径实测有数字；Megatron TP 与 vLLM PagedAttention 能讲到源码行；≥1 个开源 PR；KV Cache 卸载 benchmark |
| **3. 无人机横向** | 防御性投入，只求交差 | ~5h | 有 baseline + 1 个改动点 + 报告 |

**AI Infra 方向已定（2026-09-08）：AI 存储为主 + 训推框架优化为辅，不再等 Phase 3 后再选。** 理由见 `plan/notes/ai-infra-jd-分析.md`（22 条 JD 调研）：存储赛道上列存背景是别人拿不走的差异化，等 3 个月等于闲置唯一能立刻变现的资产。**不做算子方向**（从零跟科班 GPU 选手拼，无优势），CUDA 只学到"能读能改 kernel"。

**两条线不再是两条**：Agent Memory 是主线 A/B 的**问题来源与应用场景**，主线 A/B 是论文 idea 的实现验证端。见路线图 v2 第五节的交叉表（记忆条目≡行记录+索引、KV Cache≡最短期的记忆、记忆淘汰≡缓存替换）。一份时间两份产出。

**横向硬约束**：不占用工作日下午块 1；不为横向加班；不做"感兴趣的优化"。

---

## 4. 时间与节奏

| 时段 | 时长 | 内容 |
|---|---|---|
| 上午 · 健身 | 1~1.5h | 论文 L0 速览 20~30 篇 + 横向回消息（手机/平板，只做输入） |
| 下午块 1 | 2h | **主线 A 存储 / IO**（W1-W3、W11-W14）或 **CUDA**（W4-W6），只做一件事，断网通知 |
| 下午块 2 | 1.5h | **主线 B 训推框架源码**（W7-W10）或 论文 L2 精读 + 写 idea |
| 下午块 3 | 45min | 面经一题 + 打卡 |
| 晚上 | 0~1h | **只在白天没拿下时开**，不新启动任务 |

- **每周有效时间按 12h 排，允许每周 2 天为 0**（研一上有课 + 组会 + 事务，09-07 全天入学仪式是常态不是意外）。别按理想排计划，崩一周全线崩。
- 横向锁死 **周三下午 + 周六上午**，各 2~3h，其余时间不碰
- 周一早上加 20 分钟定本周三件事；周日晚上 30 分钟周复盘

---

## 5. 文件地图（单一事实来源）

| 路径 | 内容 | 谁维护 |
|---|---|---|
| **`AGENTS.md`**（本文件） | 全局准则 | 我改，用户审核 |
| `plan/README.md` | 总规划 | 我 |
| `plan/progress.md` | 阶段验收清单 + idea 计数 + 周度日志 | 我写周复盘，**用户打勾** |
| `plan/daily/YYYY-MM-DD.md` | 日报（卡片 + 论文 + 排程 + 打卡） | 我生成骨架，**用户填打卡** |
| `plan/ideas.md` | idea 台账（唯一编号来源） | 用户写，我整理 |
| `plan/exercises.md` | 编程练习台账 | 用户写，我整理 |
| `plan/cards-index.md` | 已出知识点 / 面经题索引（**去重用**） | 我 |
| `plan/notes/<主题>.md` | 深挖产出 | 我 |
| `data/dailyimprove.db` | 系统状态（去重、来源评分、运行记录） | 自动 |
| `data/digest/*.md` | 每次推送的产物 | 自动 |
| **`test1/test1/`** | **学习平台前端**（知识展示 / 课程 / 打卡 / 练习） | 我 |

---

## 6. 系统每天怎么跑（操作手册）

**结论：不需要用户每天手动运行。** 三层各司其职：

| 层 | 做什么 | 怎么触发 |
|---|---|---|
| ① 信息抓取 | 抓 RSS / arXiv，去重 + LLM 筛选 + 选材 | `scripts/run_daily.py`（被自动化调用） |
| ② 日报生成 | **当日收尾总结 + 次日日报**（面经卡片 + 论文速览 + 排程） | WorkBuddy 自动化 **每天 22:00**（先关今天，再铺明天） |
| ③ 人工 | 填打卡、打勾验收清单 | 用户，每天 5 分钟 |

> **为什么是晚上 10 点而不是早上 8 点（2026-09-08 改）**：早上生成日报时，昨天的打卡往往是空的，导致「昨日结果」只能写"无法判定"。改成 22:00 后，自动化先给当天日报追加一段「当日收尾总结」（按 7.3 四档语法统计完成情况），再生成**第二天**的日报 —— 此时当天的打卡数据是最新的，「昨日结果」是真实的，不用猜。
> 副作用：日报文件名是**当天日期**，但实际是前一晚 22:00 生成的。查历史时按文件内容里的日期看，不要按文件 mtime 看。

**用户只需要做两件事**：
1. 想看 Web 控制台时：`./start.sh` 后开 http://127.0.0.1:8000（**它只在进程运行时才定时触发**，关机就停）
2. 每天填日报底部的 5 行打卡

**三种运行方式**（任选，互不冲突）：

```bash
# A. 常驻服务（Web 控制台 http://127.0.0.1:8000 + APScheduler 自动定时）
./start.sh

# B. 手动跑一次，不依赖常驻进程（推荐，也是自动化调用的方式）
.venv/Scripts/python.exe scripts/run_daily.py --all --summary

# C. 只跑某个模块
.venv/Scripts/python.exe scripts/run_daily.py agent-memory-papers --summary

# 其他常用
.venv/Scripts/python.exe scripts/run_daily.py --list     # 列出模块（不写数据，安全）
.venv/Scripts/python.exe scripts/check_sources.py        # 源体检
.venv/Scripts/python.exe scripts/reset_items.py --yes    # 清空抓取产物
```

> Windows 下用 `.venv/Scripts/python.exe`；`start.sh` 走的是 `python3`，首次运行会把依赖装到 `.deps`（缺 trafilatura，正文提取会降级，不影响主流程）。

**为什么推荐 B**：Windows 关机/休眠会让常驻进程停掉，B 由自动化在需要时唤起，更可靠；且会把结果写进 `plan/daily/<日期>-sources.md`，日报直接消费。

---

## 6.5 学习平台（知识 / 课程 / 打卡 / 练习）

前端在 `test1/test1/`（Vite + React），数据来自后端新增的 `/api/study/*`。**开两个终端**：

```bash
# 终端 1：后端（必须常开，前端所有数据都从这来）
.venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000

# 终端 2：前端
cd test1/test1
npx vite --host 127.0.0.1 --port 4173
```

浏览器打开 **http://127.0.0.1:4173**。

| 页签 | 内容 |
|---|---|
| **今日** | 从 `plan/daily/` 最新日报解析三件事与打卡清单；四档状态勾选 + 说明，**保存后同时写 `checkins` 表并回写日报 md** |
| **知识** | 论文/博客卡片流。展示 LLM 生成的中文摘要、判断理由、标签、评分；想深入再点底部原文链接 |
| **课程** | 课程主页、介绍、相关资料（课件/作业/代码），按 track / phase 分组 |
| **练习** | 面经自测（选题 → 提交 → 看解析），题库在 `test1/test1/src/data/interview.ts` |

**踩过的坑（别重蹈）**：
1. **必须加 `--host 127.0.0.1`** —— 不加 vite 只监听 IPv6 `[::1]`，用 127.0.0.1 打不开。
2. **必须设 `NO_PROXY=127.0.0.1,localhost`** —— 环境里有 `HTTP_PROXY`，vite 的 proxy 会走沙箱代理，转发到本机 8000 被拒（502 `upstream connect failed`）。
3. **vite 读 `vite.config.js` 而不是 `.ts`** —— 前者是 `tsc -b` 的编译产物且优先级更高。改 proxy 配置**两个文件都要改**。
4. 后端改了模型或 API 后**必须重启**；`checkins` / `courses` 表由 `init_db()` 自动建。
5. 课程数据用 `python scripts/seed_courses.py` 导入（按 title 去重，可重复跑）。

---

## 7. 接续机制：不脱节、不重复

### 7.1 不脱节 —— 靠日报的固定字段

每份日报**必须**包含（顺序固定）：

```
## 昨日承诺        ← 从昨天日报的「明日预告」原样搬过来
## 昨日结果        ← 从昨天「今日打卡」读取，未完成的标出原因
## 今日三件事      ← 论文 / AI Infra / 横向各一件，具体到章节或论文名
## 明日预告        ← 明天日报会原样搬到「昨日承诺」
```

**生成日报的强制前置动作**（我必须做，不能省）：
1. 读 `plan/progress.md` —— 确认当前 Phase 与周次
2. 读 `plan/daily/` 最近 3 个文件 —— 提取未完成的承诺与卡点
3. 读 `plan/cards-index.md` —— 避免重复出题
4. 读 `plan/exercises.md` —— 练习进度联动

### 7.2 不重复 —— 四道去重

| 对象 | 去重机制 |
|---|---|
| 知识点 / 面经题 | `plan/cards-index.md`，出卡前必查；答错的标红，第二天间隔复习一次 |
| 论文 | DailyImprove **persistent 永久去重**（推过不再推）+ 日报里标注"已推送过" |
| idea | `plan/ideas.md` 连续编号 #001…，唯一台账 |
| 编程练习 | `plan/exercises.md` 状态机：待做 → 进行中 → 卡住 → 已通过 |

### 7.3 四档状态语义（[!] vs [?] 别再用反了）

| 标记 | 语义 | 必须配套 |
|---|---|---|
| `[x]` | 已完成 | — |
| `[ ]` | 未做 | — |
| `[!]` | **部分完成，主动顺延**（做了一部分可验证的，或主动换轨做了别的） | 在「明日预告」里写顺延项的下一步 |
| `[?]` | **卡住被动阻塞**（试过、降级、不知道继续） | note 写"卡在哪 / 试过什么 / 降级方案是什么"；超过 2 天触发强制降级 |

判定优先问自己：「这件事**现在不做的原因**是什么」——A) 我主动调整优先级让别的更优先 = `[!]`；B) 我想推进但**能力/环境/资料**挡了我 = `[?]`。两者都是真信号但要求不同动作（`[!]` 只需挂明日预告；`[?]` 需要降级方案）。

### 7.3 进度标记的四档语法（统一）

**`plan/daily/YYYY-MM-DD.md` 的「今日打卡」**只用这四种 checkbox 语法，用户不要再造新的：

```
- [x] AI Infra：E02 复现完成（耗时 1.2h，见 plan/notes/fs.md）
- [ ] 论文 idea：未产出，明日块 2 继续
- [!] E01：跑了一半（row 遍历测完，col 待测），顺延至明日块 1
- [?] 卡住：perf c2c 在 WSL 无 PMU 支持，已降级为耗时对比
```

含义约定：
- `[x]` 已完成 / `[ ]` 未做（区分清楚，不要混用模糊语）
- `[!]` 部分完成且**跨天延续**——必须同步在「明日预告」里写明衔接点
- `[?]` 卡点——必须写清楚卡在哪、试过什么、降级方案

**`plan/exercises.md` 复盘栏**用 4 行简记：`状态 / 复盘（含产出路径） / 卡点 / 耗时`。

**`plan/progress.md` 验收清单**用 `🚧 [ ] ...` 前缀标跨周项，跨周段必须在下一周「计划」里复述一次。

**触发"明日预告"写入的三种情况**：
1. 当日 `[!]` 项的衔接点（"明日块 1 接续 E01 的 col 遍历"）
2. 当日 `[?]` 项的降级方案（"明日改用 VTune 复测"）
3. 当日三件事里有任何一件未完成时，自动挂到「明日预告」

---

## 8. 内容质量怎么保证

1. **源体检**：每月一次 `python scripts/check_sources.py`，看哪些源失效了。
   - 注意：本仓库所在环境若走代理，源"失败"不代表真失效，换机器再验。
2. **LLM 筛选**：阈值默认 60（论文模块 62）。每周扫一眼 `data/digest/`，如果连续出现不相关条目，说明 profile 写得太宽，收紧 `llm.profile`。
3. **反馈闭环**：Web 控制台 `/sources` 给来源打 1~5 星，评分影响后续排序。低分源会自动降频。
4. **内容三问**：每篇推过来的论文只问——解决了什么 / 代价是什么 / 什么情况下会失效。
5. **我的输出约束**：不确定必须标注，禁止编造（见第 2 节）。
6. **月度抽检**：看本月推送里有多少条真的读了。低于 30% 说明模块该砍或调窄。

### 已知：LLM 配置（2026-09-06 修复）

- 模型 `deepseek-v4-flash` 是**推理型**，默认会产 `reasoning_content`，对"批量筛选 + 摘要"没必要
- 已在 `.env` 加 `LLM_DISABLE_THINKING=true`，请求带 `thinking:{"type":"disabled"}`
- 实测：关思考后 **11.3s → 4.0s**，且不再消耗推理 token
- 模块 YAML 的 `max_tokens` 已统一提到 8000，`batch_size` 降到 5
- 若以后换模型，先跑一次 `scripts/run_daily.py <模块>` 看日志里有没有 `LLM 批次筛选失败`

---

## 9. 想深入了解某个内容时

- **不紧急**：写进当天日报的「深挖队列」字段，我次日处理，产出 `plan/notes/<主题>.md`
- **紧急**：直接让我查，我当场检索并写进 `plan/notes/`，同时在当天日报挂链接
- 深挖产出必须包含：结论 + 证据来源链接 + 与现有知识的关系，不要只给结论

---

## 10. 编程练习怎么管

台账 `plan/exercises.md`，每行：

```
| # | 主题 | 来源/课程 | 状态 | 验收判据 | 产出路径 | 耗时 | 复盘 |
```

- **状态**：待做 / 进行中 / 卡住 / 已通过
- **每个练习必须有可验证的判据**（跑通 / 性能数字 / 输出正确），没有判据的练习不进台账
- **卡住超过 2 天**：必须写清楚卡点，然后降级——换更简单的版本，或标记"需要外部帮助"，不要死磕
- 产出代码统一放 `practice/`（按路线阶段分目录）

---

## 10.5 做练习时能不能用 AI（2026-09-08 定，不搞一刀切）

**判据只有一条：这个练习的价值是「能被追问」还是「能用就行」。** 能被追问的必须自己写，能用就行的随便用 AI。

| 级别 | 内容 | AI 怎么用 |
|---|---|---|
| **A 级 · 必须手搓** | sgemm 每一步、reduce/softmax/layernorm kernel、false sharing 复现、memory pool、无锁队列、PagedAttention 源码分析 | **只许当 reviewer**：写完让它挑毛病、问"为什么这步快"。**不许让它写实现** |
| **B 级 · 可辅助** | needle autograd/CNN、算子融合 pass、KV Cache 卸载实验搭建、量化脚本改造 | 可以让 AI 出框架和骨架，**但每个设计决策要能说出为什么这么做** |
| **C 级 · 随便用** | benchmark 脚本、绘图、CMake/Makefile、fio job file、数据预处理、文档注释、报错解释、环境配置 | 全交给 AI。这些是纯工具性工作，不产生学习价值，省下的时间全给 A 级 |

**三条硬规则**：
1. **卡住超过 30 分钟才许问 AI**，且问"为什么会这样"而不是"怎么写"。直接要答案等于把练习作废。
2. **做完的判据不是跑通，是能白板重写 + 回答每一步为什么**。AI 写完后讲不清为什么 = 没做，**不要在 `exercises.md` 里打勾**。
3. **A 级练习上了简历就必须自己能扛追问**。面试官三问就露馅的东西，不如不写。

**AI 在这儿的最高价值用法是当面试官，不是当写手**：写完 E02 让它追问"伪共享和带宽打满怎么区分"，比让它生成代码有用得多。

> 时间只有 12 周，不可能全手搓 —— 所以 C 级放开用是必要的，那是拿工具换时间；A 级手搓也是必要的，那是拿时间换能力。混在一起做就是两头空。

---

## 11. 每日 / 每周 / 每月清单

**每天（5 分钟）**
- [ ] 填日报底部 5 行打卡
- [ ] 有卡点就写进「卡点」栏（这是让明天接上的关键）

**每周（周日 30 分钟）**
- [ ] 更新 `plan/progress.md` 的验收清单打勾
- [ ] 补 `plan/ideas.md` 本周新增
- [ ] 更新 `plan/exercises.md` 状态
- [ ] 发横向周报（5 行模板，见 `plan/drone-contest.md`）

**每月（30 分钟）**
- [ ] `python scripts/check_sources.py` 源体检
- [ ] 归档 `plan/daily/` 上个月文件到 `plan/daily/archive/`
- [ ] 备份 `data/dailyimprove.db`
- [ ] 出一份 open problems 清单

---

## 12. 已知的坑（我踩过的，别重蹈）

1. **试跑会污染永久去重**：手动跑模块会把条目标记为 `pushed`，persistent 模块**以后再也不推**它们。试跑完用 `python scripts/reset_items.py --yes` 清干净。
2. **常驻进程不是永动机**：关机/休眠后 APScheduler 就停了，别以为配了 cron 就一定会跑。
3. **日报不填就等于没跑**：打卡是系统唯一能感知用户进度的入口，不填会导致周复盘数据为空、明日预告失真。
4. **LLM 成本**：每模块每天一次 LLM 批量调用。现在只保留 3 个模块（agent-memory-papers / ai-infra / drone-contest），`tech-blog` 和 `ai-news` 已停用省成本。
5. **微信渠道没启用**：模块 `push.channels` 目前是 `[file]`（写本地文件）。想在手机上看，改成 `[file, wechat]`（会进公众号草稿箱，需手动发布）。
6. **AirSim 已停止维护**：微软官方仓库已归档，UE4 版本兼容性坑不少。如果赛事没提供固定镜像，优先找往届选手的环境包，别自己从头配。
7. **数据库是唯一持久状态**：`data/dailyimprove.db` 定期备份，丢了所有去重历史和来源评分就没了。

---

## 13. 待确认事项

- [ ] **GPU 型号 / 显存 / 驱动 / CUDA 版本**（Phase 1 前置，**W3 末（09-26）前必须定**；显存 < 12G 需提前规划云平台或降级方案）
- [ ] **是否有 ≥2 张 NVIDIA 卡** —— 决定 Megatron TP 是真跑还是只读源码（只有 1 卡时不要谎称跑过多卡）
- [ ] **Linux 实验环境**：当前是 Windows。`fio` / `io_uring` 可用 WSL2（需实测确认内核支持），`perf c2c` 在 WSL2/虚拟机通常缺 PMU 支持，需降级为纯耗时对比
- [ ] 是否启用微信推送渠道
- [ ] 无人机赛事的 deadline 与评分规则（不影响主线，但决定横向节奏）

### 已确认（2026-09-08）

- ✅ **2026 级研一，2029 年毕业**，目标 **2027 年寒假**第一段 AI Infra 实习 → 投递启动 **2026-12-01**
- ✅ **方向已定**：AI 存储为主 + 训推框架优化为辅；不做算子方向

---

## 14. 相关文档

- `plan/README.md` — 总规划与作息
- `plan/ai-infra-roadmap.md` — **v2** 16 周学习路线（主线 A 存储 / 主线 B 框架 / 交叉点 / 裁剪顺序）
- `plan/notes/ai-infra-jd-分析.md` — 22 条真实 JD 调研与 gap 分析（方向决策的依据）
- `plan/agent-memory-research.md` — 调研 SOP、主题雷达、idea 模板、**与存储主线的交叉点**
- `plan/drone-contest.md` — 横向最小可交差路径
- `plan/progress.md` — 进度台账
